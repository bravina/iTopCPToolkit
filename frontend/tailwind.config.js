export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
  // Light is the base palette; every dark rule is a `dark:` variant, switched by
  // the `dark` class that useTheme() puts on <html>.
  darkMode: 'class',
  theme: { extend: {} },
  plugins: [],
}
